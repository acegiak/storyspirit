name: ${name}
version: ${version}
description: ${description}
author: ${author}
website: ${url}
api-version: 1.13

main: ${mainClass}

load: STARTUP

commands:
    name:
        description: Generate a name
        usage: /<command>
    spawn:
        description: Generate a new npc
        usage: /<command>

permissions: {}